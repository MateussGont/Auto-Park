#include <stdio.h>

class models
{

    typedef struct
    {
        double length;
        double width;
        double wheelbase;
    } Car;

    typedef struct
    {
        double length;
        double width;
    } ParkingSpot;
};